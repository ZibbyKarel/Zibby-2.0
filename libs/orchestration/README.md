# orchestration

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build orchestration` to build the library.

## Running unit tests

Run `nx test orchestration` to execute the unit tests via [Jest](https://jestjs.io).
